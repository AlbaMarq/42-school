#include <unistd.h>
#include <stdio.h>

int main()
{
	/*
	char c = 'a';
	int c = 97;
	write(1, &c, 1);
	*/

	// write(1, "%", 1);

	/*
	int bytes = 0;
	bytes = write(1, "1234", 630);
	printf("\n%d", bytes);
	*/

	return 0;
}